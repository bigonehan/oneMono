# template/web/next

Monorepo Next app template.

## Rules
- Add UI components in `packages/ui/shadcn`.
- Import UI only via `@ui/shadcn`.

## Commands
```bash
bun run dev
bun run check-types
bun run build
```
