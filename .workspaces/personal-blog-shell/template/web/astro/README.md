# template-web-astro

Astro template app for this monorepo. It uses `@ui/shadcn` for `Header`/`Footer` and provides a starter page with header/body/footer structure.

## Scripts

- `bun run dev`
- `bun run build`
- `bun run preview`
- `bun run check-types`
